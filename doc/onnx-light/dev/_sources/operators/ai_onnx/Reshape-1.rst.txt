.. _op_ai_onnx_Reshape-1:

Reshape - version 1
===================

This page documents version **1** of operator **Reshape**. See :doc:`Reshape` for the latest version (since version 25).

- **Domain**: ``ai.onnx``
- **Since version**: 1

Reshape the input tensor similar to numpy.reshape.
It takes a tensor as input and an argument ``shape``. It outputs the reshaped tensor.
At most one dimension of the new shape can be -1. In this case, the value is
inferred from the size of the tensor and the remaining dimensions. A dimension
could also be 0, in which case the actual dimension value is unchanged (i.e. taken
from the input tensor). Shape (second input) could be an empty shape, which means converting to a scalar.
The input tensor's shape and the output tensor's shape are required to have the same number of elements.

**Inputs**

- **data** (*T*): An input tensor.

**Outputs**

- **reshaped** (*T*): Reshaped data.

**Attributes**

- **consumed_inputs** (*int[]*): legacy optimization attribute.
- **shape** (*int[]*): New shape

**Type Constraints**

- **T**: Constrain input and output types to float tensors.
  Allowed types: tensor(double), tensor(float), tensor(float16).
