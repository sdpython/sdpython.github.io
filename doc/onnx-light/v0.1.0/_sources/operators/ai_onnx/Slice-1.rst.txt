.. _op_ai_onnx_Slice-1:

Slice - version 1
=================

This page documents version **1** of operator **Slice**. See :doc:`Slice` for the latest version (since version 13).

- **Domain**: ``ai.onnx``
- **Since version**: 1

Produces a slice of the input tensor along multiple axes. Similar to numpy:
https://numpy.org/doc/stable/reference/routines.indexing.html
Slices uses ``axes``, ``starts`` and ``ends`` attributes to specify the start and end
dimension for each axis in the list of axes, it uses this information to
slice the input ``data`` tensor. If a negative value is passed for any of the
start or end indices, it represent number of elements before the end of that
dimension. If the value passed to start or end is larger than the ``n`` (the
number of elements in this dimension), it represents ``n``. For slicing to the
end of a dimension with unknown size, it is recommended to pass in ``INT_MAX``.
If ``axes`` are omitted, they are set to ``[0, ..., ndim-1]``.
Example 1:
  data = [
      [1, 2, 3, 4],
      [5, 6, 7, 8],
  ]
  axes = [0, 1]
  starts = [1, 0]
  ends = [2, 3]
  result = [
      [5, 6, 7],
  ]
Example 2:
  data = [
      [1, 2, 3, 4],
      [5, 6, 7, 8],
  ]
  starts = [0, 1]
  ends = [-1, 1000]
  result = [
      [2, 3, 4],
  ]

**Inputs**

- **data** (*T*): Tensor of data to extract slices from.

**Outputs**

- **output** (*T*): Sliced data tensor.

**Attributes**

- **axes** (*int[]*): Axes that `starts` and `ends` apply to. It's optional. If not present, will be treated as [0, 1, ..., len(`starts`) - 1].
- **ends** (*int[]*): Ending indices (exclusive) of corresponding axis in axes`
- **starts** (*int[]*): Starting indices of corresponding axis in `axes`

**Type Constraints**

- **T**: Constrain input and output types to all tensor types.
  Allowed types: tensor(bool), tensor(complex128), tensor(complex64), tensor(double), tensor(float), tensor(float16), tensor(int16), tensor(int32), tensor(int64), tensor(int8), tensor(string), tensor(uint16), tensor(uint32), tensor(uint64), tensor(uint8).
